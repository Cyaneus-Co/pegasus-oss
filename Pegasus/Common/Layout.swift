import SwiftUI

struct Layout {
    
    static let screenWidth = UIScreen.main.bounds.width
    static let screenHeight = UIScreen.main.bounds.height
    
    static let contentWidth = screenWidth - 8
    
    static let imageWidth = contentWidth
    static let imageHeight = imageWidth * 1.333
}
